[project]
name = "pidroid-core"
version = "0.1.0"
description = "Shared code and components of the Pidroid bot."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.4",
]
